import 'package:flutter/material.dart';

class StorScreen extends StatelessWidget {
  const StorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Store Screen'),
    );
  }
}
